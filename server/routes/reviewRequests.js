/* ── RETIRED ───────────────────────────────────────────────────────
   No longer mounted in server.js — superseded by the direct "Create
   Scratch Card" flow in routes/rewards.js. Kept only as a no-op stub
   exporting an empty router. */
const express = require('express');
module.exports = express.Router();
