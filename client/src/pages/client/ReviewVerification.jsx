/* ── RETIRED ───────────────────────────────────────────────────────
   The Review Verification approval-queue page has been replaced by
   the FINAL Customer Reward Workflow: the business owner now creates
   the Scratch Card directly from Reward Management (Reward Management
   → Create Scratch Card) after manually verifying the customer's
   Google Review in person. No route points here anymore. */
export default function ReviewVerification() {
  return null;
}
